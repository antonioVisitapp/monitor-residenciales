version: "3.9"

services: 
   monitoreo:
    build: .
    extra_hosts:
      - "host.docker.internal:host-gateway"
    container_name: visitapp_monitor    
    ports:
      - "3000:3000"
    restart: always
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"






